[user]
	email = cauebs@pm.me
	name = Cauê Baasch de Souza

[core]
	pager = delta

[interactive]
	diffFilter = delta --color-only

[delta]
	features = side-by-side line-numbers
	syntax-theme = gruvbox
[init]
	defaultBranch = main
[pull]
	ff = only
